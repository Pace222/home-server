services:
  int-proxy:
    build: .
    image: caddy-acme-dns
    container_name: int-proxy
    read_only: true
    restart: unless-stopped
    env_file:
      - ${SECRETS_DIR:?}/int-proxy/.env
    environment:
      MY_DOMAIN: "${MY_DOMAIN:?}"
    ports:
      - "80:80"
      - "443:443/tcp"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ${CONFIGS_DIR:?}/error-pages/dist:/errors:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      net-int-homepage:
        ipv4_address: 172.19.2.2
      net-pihole:
        ipv4_address: 172.19.3.2
      outgoing:
        ipv4_address: 172.19.0.130
    labels:
      homepage.group: "Internal"
      homepage.name: "Caddy Reverse Proxy"
      homepage.icon: "caddy.png"
      homepage.description: "Reverse proxy for internal services."
      homepage.instance.internal.widget.type: "caddy"
      homepage.instance.internal.widget.url: "http://int-proxy:2019"

volumes:
  caddy_data:
  caddy_config:

networks:
  net-pihole:
    external: true
  net-int-homepage:
    external: true
  outgoing:
    ipam:
      config:
        - subnet: 172.19.0.0/24
          gateway: 172.19.0.129
