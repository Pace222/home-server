# Comment me
echo "Edit ~/.serverrc and /etc/network/interfaces.d/100-ip-setup as needed"

export MY_DOMAIN="..."

# Directories
export CONFIGS_DIR="..."
export SERVICES_DIR="..."
export SECRETS_DIR="..."
export DOCKERS_DIR="..."
export SLOW_VOLUMES="..."
export FAST_VOLUMES="..."
alias configs="cd $CONFIGS_DIR"
alias services="cd $SERVICES_DIR"
alias secrets="cd $SECRETS_DIR"
alias dockers="cd $DOCKERS_DIR"
alias slow_volumes="cd $SLOW_VOLUMES"
alias fast_volumes="cd $FAST_VOLUMES"

# Networking
export PROXMOX_IP="..."
export DNS_IP="..."
export VPN_IP="..."
export EXT_PROXY_IP="..."
export INT_PROXY_IP="..."

# Secrets
source $SECRETS_DIR/.env-compose
