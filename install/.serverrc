export MY_DOMAIN="..."

# Directories
export CONFIGS_DIR="..."
export SERVICES_DIR="..."
export SECRETS_DIR="..."
export DOCKERS_DIR="..."
export SLOW_VOLUMES="..."
export FAST_VOLUMES="..."
export STORAGE_DIR="..."
alias configs="cd $CONFIGS_DIR"
alias services="cd $SERVICES_DIR"
alias secrets="cd $SECRETS_DIR"
alias dockers="cd $DOCKERS_DIR"
alias slow_volumes="cd $SLOW_VOLUMES"
alias fast_volumes="cd $FAST_VOLUMES"
alias storage="cd $STORAGE_DIR"

# Networking
export LAN_SUBNET="..."
export HOST_IP="..."
export OPNSENSE_SUBNET_IP="..."
export PROXMOX_IP="..."
export OPNSENSE_LAN_IP="..."

# Secrets
source $SECRETS_DIR/.env-compose
